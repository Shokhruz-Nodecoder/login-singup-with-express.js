class Regist {
    // #password 
    constructor(username, password,id) {
            this.username = username;
            this.password = password;
            this.id = id;
        }
}

module.exports = Regist;